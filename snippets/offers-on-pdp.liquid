<div class="offer-container">
  <!-- Offer cards that will be horizontally scrollable -->
  <div class="offer-card" onclick="showDetails('offer1')">
    <div class="offer-card-content">
      <picture>
        <source
          media="(max-width: 768px)"
          srcset="https://cdn.shopify.com/s/files/1/0689/3014/2515/files/MBK_MBK-Icon-Logo_Blue.png?v=1738306359"
        >
        <img
          class="offer-image"
          src="https://cdn.shopify.com/s/files/1/0689/3014/2515/files/MBK_MBK-Logo_Blue-_1.png?v=1738306360"
          alt="Offer Image"
        >
      </picture>
      <p class="offer-text">Pay via mobikwik and get upto Rs. 1000 cashback<br>
      <small>view offer details</small></p>
    </div>
  </div>

  
</div>

<!-- Modal to show details on click -->
<div id="offer1-details" class="modal">
  <div class="modal-content">
    <span class="close-button" onclick="closeModal('offer1')">&times;</span>
    <h2>Offer Details</h2>
    <ol>
      <li>Offer valid on min. Transaction of Rs. 2500/-</li>
      <li>Offer can be availed only once per user in a month</li>
      <li>Cashback shall be credited within 24 hours of transaction</li>
      <li>Offer is valid till 31st March 2025</li>
      <li>Other T&Cs may apply</li>
    </ol>
  </div>
</div>

<style>
    /* Styling for the offer container to make it horizontally scrollable */
  .offer-container {
      display: flex;
      overflow-x: auto;
      gap: 20px;
      padding: 10px;
      flex-wrap: nowrap;
  }

  .offer-card {
      border-radius: 10px;
      padding: 15px;
      cursor: pointer;
      width: 250px;
      min-width: 250px;
      display: flex;
      justify-content: flex-start;  /* Align content to the left */
      align-items: center;
      transition: transform 0.3s ease;
      box-shadow: 0 4px 6px rgba(0, 0, 0, 0.1);
  }

  .offer-card:hover {
      transform: scale(1.05);
  }

  /* Image for desktop and mobile */
  .offer-image {
      /* width: 50px; */
      height: 50px;
      object-fit: cover;
      margin-right: 10px; /* Space between image and text */
  }

  /* Mobile image styling */
  @media (max-width: 768px) {
      .offer-image {
          /* width: 40px; */
          height: 40px;
      }

      .offer-card {
          width: 200px;
          min-width: 200px;
      }
  }

  /* Text inside the offer card */
  .offer-text {
      font-size: 14px;
      color: black;
      margin-top: 10px;
      text-align: left;  /* Left align the text */
  }

  /* Modal styling */
  .modal {
      display: none;
      position: fixed;
      top: 0;
      left: 0;
      width: 100%;
      height: 100%;
      background: rgba(0, 0, 0, 0.5);
      justify-content: center;
      align-items: center;
      z-index: 1000;
  }

  .modal-content {
      background: #fff;
      padding: 20px;
      border-radius: 8px;
      width: 60%;
      max-width: 600px;
      position: relative;
  }

  .close-button {
      position: absolute;
      top: 10px;
      right: 10px;
      font-size: 30px;
      cursor: pointer;
      color: #333;
  }

  @media (max-width: 768px) {
      .modal-content {
          width: 80%;
      }
  }

    /* Style the scrollbar */
  .offer-container::-webkit-scrollbar {
      width: 8px;  /* Reduced width of the scrollbar */
      height: 8px; /* Reduced height for horizontal scrollbar */
  }

  /* Style the scrollbar track */
  .offer-container::-webkit-scrollbar-track {
      background: #f4f4f4;  /* Light background color for the track */
  }

  /* Style the scrollbar thumb */
  .offer-container::-webkit-scrollbar-thumb {
      background-color: #ffdba6; /* Set the color of the scrollbar */
      border-radius: 10px;  /* Rounded corners for the thumb */
      border: 2px solid #fff; /* Optional: add border around the thumb */
  }

  /* Style the scrollbar thumb when hovering */
  .offer-container::-webkit-scrollbar-thumb:hover {
      background-color: #e0c27b;  /* Darker color on hover */
  }
</style>

<script>
    // Function to show modal with offer details
  function showDetails(offerId) {
      document.getElementById(offerId + '-details').style.display = 'flex';
  }

  // Function to close the modal
  function closeModal(offerId) {
      document.getElementById(offerId + '-details').style.display = 'none';
  }
</script>
